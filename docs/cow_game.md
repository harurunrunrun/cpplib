---
title: Cow Game
documentation_of: ../src/algorithm/graph/cow_game.hpp
---

????

```text
d_to - d_from <= cost
```

?????`d_v - d_source` ?????????

?????? `from -> to`??? `cost` ??????????? `source` ???????????

# ??

```cpp
cow_game<T>(n, source, constraints, inf)
```

`constraints` ? `CowGameConstraint<T>{from, to, cost}` ???

`CowGameResult<T>` ??????

```cpp
bool feasible;
vector<T> maximum;
vector<char> bounded;
```

????????????????????? `feasible == false`?

`bounded[v] == true` ????`maximum[v]` ? `d_v - d_source` ?????

## ?????

- $O(NM)$
