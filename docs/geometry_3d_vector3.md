---
title: 3D Vector Alias (三次元ベクトル別名)
documentation_of: ../src/algorithm/geometry/3d/vector3.hpp
---

## API

- `Vector3`: `Point3` の型別名。点とベクトルで同じ座標演算を利用できる。

## API別の時間計算量・空間計算量

- 型別名なので追加の時間・領域を使わない。

## 注意点

`Vector3` と `Point3` は同一型であり、型システム上は区別されない。
