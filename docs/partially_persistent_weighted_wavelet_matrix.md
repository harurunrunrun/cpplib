---
title: Partially Persistent Weighted Wavelet Matrix
documentation_of: ../src/structure/wavelet_matrix/partially_persistent_weighted_wavelet_matrix.hpp
---

部分永続の重み付きwavelet matrix。更新は最新版に対してのみ行う。

# 更新

```cpp
wm.set(k, value, weight)
wm.set_value(k, value)
wm.set_weight(k, weight)
```

# 時間計算量

`PersistentWeightedWaveletMatrix` と同じ。
