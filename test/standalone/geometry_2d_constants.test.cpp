// competitive-verifier: STANDALONE

#include "../../src/algorithm/geometry/2d/constants.hpp"
#include "geometry_2d_test_common.hpp"

int main(){
    int queries;
    std::cin >> queries;
    while(queries-- > 0){
        std::cout << (GEOMETRY_EPS == 1e-10L) << '\n';
    }
}
